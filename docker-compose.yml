version: '3.8'
services:
  frontend:
    build: ./dsa_front
    ports:
      - "3000:3000"
    volumes:
      - ./dsa_front:/app
      - /app/node_modules

  backend:
    depends_on:
      db:
        condition: service_healthy
    build: ./dsa_back
      # うまくいかなかったから仕方なくvolumesでマウント．本当はローカルに反映させたくないからCOPY使いたい．
      # context: .
      # dockerfile: dsa_back/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./dsa_back:/app
      - ./dsa_uploaded_file:/uploaded_file
      - ./dsa_resource:/resource
    environment:
      DATABASE_HOST: db
      DATABASE_NAME: dsa 
      DATABASE_USER: dsa 
      DATABASE_PASSWORD: dsa-jikken 
      DATABASE_PORT: 3306

  judge:
    depends_on:
      db:
        condition: service_healthy
    build: ./dsa-judge
    ports:
      - "8080:8080"
    volumes:
      # ホスト環境のDockerサービスにアクセスするためのDockerソケットのマウント
      # 注) ホスト環境がUbuntuである必要あり
      - /var/run/docker.sock:/var/run/docker.sock
      # ソースコードのマウント
      - ./dsa-judge/src:/app
      # リソースディレクトリ(アップロードされたファイル、テストケース、サンプルプログラム等)のマウント
      - ./dsa_resource:/resource
      # ログディレクトリのマウント
      - ./dsa-judge/logs:/logs
      # Sandboxコンテナのメモリ使用量のリアルタイム計測のために、sysgroupディレクトリのマウント
      # 注) ホスト環境がUbuntuである必要あり
      - /sys/fs/cgroup:/sys-host/fs/cgroup
    environment:
      DATABASE_HOST: db
      DATABASE_NAME: dsa 
      DATABASE_USER: dsa 
      DATABASE_PASSWORD: dsa-jikken 
      DATABASE_PORT: 3306
      DOCKER_HOST: unix:///var/run/docker.sock
      TZ: Asia/Tokyo

  db:
    image: mysql:8.3
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    volumes:
      - db-data:/var/lib/mysql
      - ./dsa_db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./dsa_db/my.cnf:/etc/mysql/conf.d/my.cnf
    environment:
      MYSQL_ROOT_PASSWORD: dsa-root
      MYSQL_DATABASE: dsa
      MYSQL_USER: dsa
      MYSQL_PASSWORD: dsa-jikken
    ports:
      - "3306:3306"

volumes:
  db-data:

